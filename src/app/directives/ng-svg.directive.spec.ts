import { NgSvgDirective } from './ng-svg.directive';

describe('NgSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new NgSvgDirective();
    expect(directive).toBeTruthy();
  });
});
